apiVersion: v1
kind: Secret
metadata:
  name: postgresqlPassword
  labels:
    app: example-voting-app
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  postgresPassword: {{ printf "secret_%s_postgresql_postgresPassword" .Release.Namespace | b64enc | quote }}